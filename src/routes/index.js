import Router from "koa-router";
// import {DoBinding,Binding,Info,Leave,checkUnionID} from '../controllers/leaveController';

const router = Router({
    prefix: '/grpc'
})

// router.get("/binding", Binding);


export default router;